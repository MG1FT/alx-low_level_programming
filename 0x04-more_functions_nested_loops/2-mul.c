#include "main.h"

/**
 * mul - multiplies two integers
 * @a: to be multiplied with b
 * @b: to be multiplied with a
 *
 * Return: Returns (0)
 */
int mul(int a, int b)
{
	int multiply = a * b;

	return (multiply);
}
